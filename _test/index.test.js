//Import routes
//import {getHandler, postHandler} from './index.js';

//Create instance of Express application
//const app = express();

//Configure routes
//app.get('/', getHandler);
//app.post('/', postHandler);

//Test GET handler
//describe('GET /', () => {
  //test('responds with "yello there brother"', async () => {
    //const response = await request(app).get('/');
    //expect(response.status).toBe(200);
    //expect(response.text).toBe('yello there brother');
  //});
//});

//Test POST handler
//describe('POST /', () => {
  //test('responds with the received data', async () => {
    //const data = { key: 'value' };
    //const response = await request(app)
      //.post('/')
      //.send(data);
    //expect(response.status).toBe(200);
    //expect(response.text).toBe('Received: ' + JSON.stringify(data));
  //});
//});